{
  "name": "amanita",
  "private": true,
  "version": "0.1.0",
  "description": "Minimal MultiBaas event-query and webhook loop for Amanita",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "test": "npm run build --silent && node --test dist/**/*.test.js"
  },
  "dependencies": {
    "@curvegrid/multibaas-sdk": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3"
  }
}
